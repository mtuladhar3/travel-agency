import SectionHeader from "../../common/SectionHeader";

export default function TourSearchSectionHeader() {
  return (
    <SectionHeader
      centered
      label="Find Tours"
      title={["Find the Right", "Tour for You"]}
    />
  );
}
